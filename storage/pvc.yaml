kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nfs-pvc
  namespace: chamo
spec:
  selector:
    matchLabels:
      app: nginx
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
