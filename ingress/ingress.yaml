---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chamo-ingress
  namespace: chamo   
spec:
  ingressClassName: nginx
  rules:
  - host: www.chamo.local 
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: chamo-service
              port:
                number: 80
